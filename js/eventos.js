/*
function hi () {
    alert("Hola");
}

var button = document.getElementsById('submit');

button.onclick = hi;  // La función es sin paréntesis porque acá no se está ejecutando la función, si no que se está asignando a un evento
*/
